let emptiness = null // indica intencionalmente que não existe valor em uma variável.

console.log(emptiness, emptiness + 3, `o valor é ${emptiness}`)

let undef // undefined é atriuido automaticamente a está variávels

console.log(undef)
