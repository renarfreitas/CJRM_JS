/* 
O objeto window 
Tudo que é feito no JavaScript, no front-end, todos os métdos e propriedades estão armazenados no objeto window.
*/

setTimeout(()=>{
  alert('executou')
}, 2000) // Executa uma função apos quantidade de tempo estipulada.

scrollTo(0, 0) // está função recebe dois argumentos, as coordenadas x e y, do que deve ser exibido no canto superiro esquedo da tela.